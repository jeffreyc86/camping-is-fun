<h1>Welcome to the <%= @activity.name %> Page</h1>

<h3>Difficulty Level:  <%= @activity.difficulty %></h3>

<h3>Campers Currently Signed Up:</h3>

<ol>
    <% @activity.campers.each do |camper| %>
    <li><%= link_to camper.name, camper_path(camper) %></li>
    <% end %>
</ol>

<%= button_to "Sign Up a Camper for #{@activity.name}", new_sign_up_path, method: :get %>
